service: dynamoDB

provider:
  name:    aws
  runtime: nodejs6.10
  stage:   ${opt:stage, self:custom.defaultStage}
  region:  ${self:custom.stage.region}
  profile: doctorshelp-${self:provider.stage}

custom:
  defaultStage: local
  stage:        ${file(../../config/stage.yml):${self:provider.stage}}

# ********************************
# ***          DANGER          ***
# ********************************
# TABLES
resources:
  Resources:
    OrganisationsTable:     ${file(./organisations.yml)}
    UserOrganisationsTable: ${file(./userOrganisations.yml)}
    QuotesTable:            ${file(./quotes.yml)}

